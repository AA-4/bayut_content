version: 2.1 # Use version 2.1 to enable orb usage.

orbs:
  win: circleci/ubuntu-1604:202007-01 # The Windows orb give you everything you need to start using the Windows executor.

jobs:
  setEnv: # name of your job
    executor: win/default # executor type

    steps:
      # Commands are run in a Windows virtual machine environment
      - checkout
      - run: yarn
      - run: yarn workspace shared-ui add react-helmet
      - run: yarn build:schools

workflows:
  setEnv_and_build:
    jobs:
      - setEnv