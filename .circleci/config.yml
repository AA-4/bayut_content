version: 2.1 # Use version 2.1 to enable orb usage
jobs:
  ubuntuJob:
    machine:
      image: ubuntu-1604:201903-01
    steps:
      - checkout
      - run: echo "Hello, $USER!"

workflows:
  setEnv_and_build:
    jobs:
      - ubuntuJob